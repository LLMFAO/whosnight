# Database URL for local development (if using local Postgres)
DATABASE_URL="postgresql://username:password@localhost:5432/whosnight"

# Session secret for Express server (if still using server locally)
SESSION_SECRET="your-session-secret-here"

# Supabase Configuration
# Get these from your Supabase project dashboard -> Settings -> API
VITE_SUPABASE_URL="https://your-project-id.supabase.co"
VITE_SUPABASE_KEY="your-anon-public-key-here"